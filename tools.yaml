sources:
  tuva-demo-465701:
    kind: "bigquery"
    project: "tuva-demo-465701"
    location: "US"

tools:
  execute_sql_tool:
    kind: bigquery-execute-sql
    source: tuva-demo-465701
    description: Use this tool to execute sql statement.
  bigquery_list_tables_tool:
    kind: bigquery-list-table-ids
    source: tuva-demo-465701
    description: Use this tool to list tables in a dataset.
  bigquery_list_datasets_tool:
    kind: bigquery-list-dataset-ids
    source: tuva-demo-465701
    description: Use this tool to list datasets in a project.
  bigquery_get_table_tool:
    kind: bigquery-get-table-info
    source: tuva-demo-465701
    description: Use this tool to get table metadata.
  bigquery_get_dataset_tool:
    kind: bigquery-get-dataset-info
    source: tuva-demo-465701
    description: Use this tool to get dataset metadata.